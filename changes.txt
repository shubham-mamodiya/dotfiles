changes on shubham-mamodiya-ThinkPad-P51s-W10DG

installed ubuntu with third party and extra codec support.
updated and upgraded it.

installed 
gh
nvim
foot
gnome-tweaks


sudo apt install neovim tmux stow
cd dot-files/
git clone git@github.com:shubham-mamodiya-dev/dotfiles.git
(type -p wget >/dev/null || (sudo apt update && sudo apt install wget -y)) 	&& sudo mkdir -p -m 755 /etc/apt/keyrings 	&& out=$(mktemp) && wget -nv -O$out https://cli.github.com/packages/githubcli-archive-keyring.gpg 	&& cat $out | sudo tee /etc/apt/keyrings/githubcli-archive-keyring.gpg > /dev/null 	&& sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg 	&& sudo mkdir -p -m 755 /etc/apt/sources.list.d 	&& echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list > /dev/null 	&& sudo apt update 	&& sudo apt install gh -y
curl -fsSL -o - https://cli.github.com/packages/githubcli-archive-keyring.gpg | gpg --show-keys
gh 
gh auth login

git config --global user.name "shubham-mamodiya-dev"
git config --global user.email "shubham.mamodiya.dev@gmail.com"
gh repo clone shubham-mamodiya-dev/dotfiles

sudo apt install yazi

sudo apt update && sudo apt install -y ffmpeg 7zip jq poppler-utils fd-find ripgrep fzf zoxide imagemagick
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
sudo snap install yazi --classic
yazi

mkdir -p ~/.local/share/fonts/JetBrainsMono
cd /tmp
wget https://github.com/ryanoasis/nerd-fonts/releases/latest/download/JetBrainsMono.zip
unzip JetBrainsMono.zip -d ~/.local/share/fonts/JetBrainsMono
fc-cache -fv

sudo apt install gnome-tweaks

sudo apt install tlp tlp-rdw 
sudo systemctl enable --now tlp
btop
sudo prime-select on-demand

